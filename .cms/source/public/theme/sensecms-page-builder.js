(()=>{
const clone=value=>JSON.parse(JSON.stringify(value??{})),dateInput=value=>String(value||'').replace(' ','T').slice(0,16),icon=(name,cls='')=>{const el=document.createElement('i');el.dataset.lucide=name;el.className=cls;return el},button=(label,name,title=label)=>{const el=document.createElement('button');el.type='button';el.dataset.action=name;el.title=title;el.setAttribute('aria-label',title);el.append(icon(label));return el},uuid=()=>crypto.randomUUID?.()||'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g,c=>{const n=Math.random()*16|0;return(c==='x'?n:(n&3)|8).toString(16)});
const boot=()=>{
 const root=document.querySelector('[data-page-builder]');if(!root||root.dataset.bound)return;root.dataset.bound='1';window.__senseCMSBuilderLifecycle?.abort();const lifecycle=new AbortController();window.__senseCMSBuilderLifecycle=lifecycle;
 const payload=JSON.parse(root.querySelector('[data-builder-payload]').textContent),catalog=payload.catalog,patterns=payload.patterns||[],layouts=payload.layouts||[],appearances=payload.appearance||{},collectionOptions=payload.collection_options||{},languages=payload.languages,aiProviders=payload.ai_providers||[],aiActions=payload.ai_actions||{},list=root.querySelector('[data-builder-list]'),empty=root.querySelector('[data-builder-empty]'),count=root.querySelector('[data-builder-count]'),stateBox=root.querySelector('[data-builder-state]'),saveButton=root.querySelector('[data-builder-save]'),undoButton=root.querySelector('[data-builder-undo]'),redoButton=root.querySelector('[data-builder-redo]'),liveFrame=root.querySelector('[data-builder-live-frame]'),inspector=root.querySelector('[data-builder-inspector]'),qualityBox=root.querySelector('[data-builder-quality]'),reviewBox=root.querySelector('[data-builder-review]'),aiBox=root.querySelector('[data-builder-ai]'),canEdit=!!payload.permissions?.edit,canReview=!!payload.permissions?.review,canAi=!!payload.permissions?.ai;
 let version=Number(payload.page.version||0),activeLocale=(languages.find(item=>Number(item.is_default))||languages[0]||{locale:'en'}).locale,dirty=false,saving=false,saveError=false,draftSaving=false,draftError=false,draftSavedAt='',draftFingerprint='',previewFingerprint='',liveMode=false,inspectorUid='',dragUid='',open=new Set(),selected=new Set(),clipboard=payload.clipboard||null,globals=payload.globals||[],revisions=payload.revisions||[],collaboration=payload.collaboration||{workflow:{},comments:{},locks:{},open_comments:0},collaborators=payload.collaborators||[],revisionSelection=new Set(),quality=payload.quality||null,qualityLoading=false,qualityFingerprint='',aiProposal=null,aiBaseFingerprint='',aiAbort=null,shareTarget=null,renameTarget=null,layoutTarget=null,mediaTarget=null,mediaPage=1,mediaTimer=0,historyTimer=0,draftTimer=0,draftMaxTimer=0,liveTimer=0,qualityTimer=0,collaborationTimer=0,lockTimer=0,lockedUid='',history=[],historyIndex=-1,blocks=(payload.blocks||[]).filter(block=>catalog[block.type]).map(normalize),savedFingerprint='';root.classList.toggle('is-review-only',!canEdit);lifecycle.signal.addEventListener('abort',()=>{clearTimeout(mediaTimer);clearTimeout(historyTimer);clearTimeout(draftTimer);clearTimeout(draftMaxTimer);clearTimeout(liveTimer);clearTimeout(qualityTimer);clearTimeout(collaborationTimer);clearInterval(lockTimer);aiAbort?.abort()},{once:true});
 if(blocks[0])open.add(blocks[0].uid);
 function localeDefaults(definition,locale){return clone(definition.localized_defaults?.[locale]||definition.defaults)}
 function defaultLayout(){const point=()=>({span:12,order:0,hidden:false});return{group:null,mode:'stack',container:'content',gap:'medium',align:'stretch',wrap:true,desktop:point(),tablet:point(),mobile:point()}}
 function defaultAppearance(){return{variant:'default',surface:'default',spacing:'default',radius:'default',align:'default'}}
 function normalizeAppearance(value){const result=defaultAppearance(),input=value&&typeof value==='object'?value:{};Object.entries(appearances).forEach(([key,definition])=>{const choice=String(input[key]||'default');result[key]=Object.prototype.hasOwnProperty.call(definition.options||{},choice)?choice:'default'});return result}
 function normalizeLayout(value){const base=defaultLayout(),input=value&&typeof value==='object'?value:{},allowed=(value,values,fallback)=>values.includes(value)?value:fallback,point=name=>{const item=input[name]&&typeof input[name]==='object'?input[name]:{};return{span:Math.max(1,Math.min(12,Number(item.span)||12)),order:Math.max(-20,Math.min(20,Number(item.order)||0)),hidden:!!item.hidden}};return{group:/^[a-f0-9-]{36}$/.test(String(input.group||''))?String(input.group):null,mode:allowed(input.mode,['stack','columns','grid','sidebar'],base.mode),container:allowed(input.container,['content','wide','full'],base.container),gap:allowed(input.gap,['none','small','medium','large'],base.gap),align:allowed(input.align,['start','center','end','stretch'],base.align),wrap:input.wrap!==false,desktop:point('desktop'),tablet:point('tablet'),mobile:point('mobile')}}
 function normalize(block){const definition=catalog[block.type],localized={};languages.forEach(language=>localized[language.locale]=Object.assign(localeDefaults(definition,language.locale),clone(block.localized?.[language.locale]||{})));return{uid:block.uid||uuid(),type:block.type,source:block.source_theme||block.source||definition.source,global_section_id:Number(block.global_section_id||0)||null,global_name:String(block.global_name||''),global_version:Number(block.global_version??block.version??0),visible:block.visible===true||Number(block.visible)===1,visible_from:dateInput(block.visible_from),visible_until:dateInput(block.visible_until),layout:normalizeLayout(block.layout),appearance:normalizeAppearance(block.appearance),shared:Object.assign(clone(definition.shared_defaults),clone(block.shared||{})),localized}}
 const request=async(url,options={})=>{const response=await fetch(url,{...options,headers:{Accept:'application/json','X-SenseCMS-Request':'1',...(options.headers||{})}}),result=await response.json();if(!response.ok||!result.ok){const error=new Error(result.message||'The operation could not be completed.');error.status=response.status;throw error}return result};
 const toast=(type,message)=>window.SenseCMSUI?.toast(type,message),blockPayload=()=>blocks.map(({uid,type,source,global_section_id,global_version,visible,visible_from,visible_until,layout,appearance,shared,localized})=>({uid,type,source,global_section_id,global_version,visible,visible_from,visible_until,layout,appearance,shared,localized})),fingerprint=()=>JSON.stringify(blockPayload());
 function updateHistoryControls(){undoButton.disabled=!canEdit||historyIndex<1;redoButton.disabled=!canEdit||historyIndex<0||historyIndex>=history.length-1}
 function updateState(){const current=fingerprint(),recovered=dirty&&draftFingerprint===current&&!draftError,label=!canEdit?'Review mode':saving?'Saving page…':draftSaving?'Saving recovery draft…':saveError?'Page save needs attention':draftError?'Recovery draft unavailable':recovered?'Recovery draft saved':dirty?'Unsaved changes':'All changes saved',detail=!canEdit?'Comment, assign and approve without changing content':saving?'Creating a new page revision':draftSaving?'Private to your account':saveError?(recovered?'Recovery draft remains safe':'Review the validation message'):draftError?'Changes remain in this browser':recovered?(draftSavedAt?'Saved '+new Date(String(draftSavedAt).replace(' ','T')).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}):'Safe to restore after reopening'):dirty?'Autosave will run shortly':'Manual save creates a page revision';stateBox.className='sensecms-builder-save-state'+(!canEdit?' is-review':saving||draftSaving?' is-saving':saveError||draftError?' is-error':recovered?' is-recovered':dirty?' is-dirty':'');stateBox.querySelector('b').textContent=label;stateBox.querySelector('[data-builder-state-detail]').textContent=detail;saveButton.disabled=!canEdit||saving||!dirty}
 function recordHistory(){clearTimeout(historyTimer);historyTimer=setTimeout(()=>{const current=fingerprint();if(history[historyIndex]===current)return;history=history.slice(0,historyIndex+1);history.push(current);if(history.length>50)history.shift();historyIndex=history.length-1;updateHistoryControls()},350)}
 function scheduleDraft(){clearTimeout(draftTimer);if(!canEdit||!dirty)return;draftTimer=setTimeout(()=>flushDraft(),2500);if(!draftMaxTimer)draftMaxTimer=setTimeout(()=>{draftMaxTimer=0;flushDraft()},15000)}
 function setDirty(value=true,{remember=true,recover=true}={}){if(!canEdit)return;const wasDirty=dirty;dirty=!!value&&fingerprint()!==savedFingerprint;if(dirty){saveError=false;draftError=false;if(remember)recordHistory();if(recover)scheduleDraft();scheduleQuality();if(liveMode){clearTimeout(liveTimer);setLiveStatus('Updating private preview…','is-updating');liveTimer=setTimeout(()=>flushDraft({quiet:true}),700)}}else if(wasDirty&&draftFingerprint)discardDraft(true);updateState()}
 function applyHistory(index){if(index<0||index>=history.length||index===historyIndex)return;clearTimeout(historyTimer);historyIndex=index;blocks=JSON.parse(history[index]).filter(block=>catalog[block.type]).map(normalize);open=new Set([...open].filter(uid=>blocks.some(block=>block.uid===uid)));if(!open.size&&blocks[0])open.add(blocks[0].uid);render();setDirty(true,{remember:false});updateHistoryControls()}
 async function flushDraft(options={}){if(!canEdit)return;if(!dirty){if(liveMode&&previewFingerprint!==fingerprint())refreshLivePreview();return}clearTimeout(draftTimer);clearTimeout(draftMaxTimer);clearTimeout(liveTimer);draftMaxTimer=0;if(draftSaving){scheduleDraft();return}const current=fingerprint();if(current===draftFingerprint){if(liveMode&&previewFingerprint!==current)refreshLivePreview();updateState();return}draftSaving=true;draftError=false;updateState();try{const result=await request('/content/builder/'+payload.page.id+'/draft',{method:'POST',keepalive:!!options.keepalive,headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,version,blocks:blockPayload()})});if(dirty){draftFingerprint=current;draftSavedAt=result.data.updated_at||''}if(liveMode){previewFingerprint=current;refreshLivePreview()}}catch(error){if(dirty){draftError=true;setLiveStatus('Preview update failed','is-error');if(!options.quiet)toast('error',error.message||'The recovery draft could not be saved.')}}finally{draftSaving=false;if(!dirty)draftError=false;updateState()}}
 async function discardDraft(quiet=false){clearTimeout(draftTimer);clearTimeout(draftMaxTimer);draftMaxTimer=0;try{await request('/content/builder/'+payload.page.id+'/draft/discard',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf})});draftFingerprint='';draftSavedAt=''}catch(error){if(!quiet)toast('error',error.message||'The recovery draft could not be discarded.')}}
 const filled=value=>Array.isArray(value)?value.length>0:String(value??'').trim()!=='';
 const coverage=block=>{const fields=catalog[block.type].fields,all=fields.length*languages.length||1,done=languages.reduce((total,language)=>total+fields.filter(field=>filled(block.localized[language.locale]?.[field.key])).length,0);return Math.round(done/all*100)};
 const blockTitle=block=>String(block.localized[activeLocale]?.title||block.localized[activeLocale]?.eyebrow||catalog[block.type].label);
 function scheduleQuality(immediate=false){clearTimeout(qualityTimer);qualityTimer=setTimeout(()=>loadQuality(),immediate?0:650)}
 async function loadQuality(force=false){const current=fingerprint();if(qualityLoading)return scheduleQuality();if(!force&&quality&&qualityFingerprint===current){renderQuality();return}qualityLoading=true;renderQuality();try{const result=await request('/content/builder/'+payload.page.id+'/quality',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,blocks:blockPayload()})});quality=result.data;qualityFingerprint=current}catch(error){toast('error',error.message||'The quality inspection could not be completed.');quality={ready:false,score:0,counts:{error:1,warning:0,advice:0},issues:[{severity:'error',category:'System',message:error.message||'The quality inspection could not be completed.'}]}}finally{qualityLoading=false;renderQuality()}}
 function qualityTarget(issue){if(issue.block_uid){if(issue.locale&&languages.some(language=>language.locale===issue.locale)){activeLocale=issue.locale;localeNavigation()}selectCanvasBlock(issue.block_uid,true);return}const url=issue.target==='seo'?'/seo?document='+payload.page.id:issue.target==='page'?'/content/pages/'+payload.page.id+'/edit':'';if(!url)return;const go=()=>location.assign(url),guard=window.SenseCMSUnsavedGuard;if(!dirty)go();else if(guard?.confirmLeave)guard.confirmLeave(go);else window.SenseCMSUI?.modal('Leave unsaved changes?','Your recent Page Builder changes have not been saved.',go)}
 function renderQuality(){if(!qualityBox)return;const actionCount=root.querySelector('[data-builder-quality-action-count]'),tabCount=root.querySelector('[data-builder-quality-count]');if(qualityLoading&&!quality){qualityBox.innerHTML='<div class="sensecms-builder-global-empty"><i data-lucide="loader-circle"></i><strong>Checking the current draft</strong><span>Core is reviewing content, accessibility, SEO and responsive behaviour.</span></div>';return}const report=quality||{ready:true,score:100,counts:{error:0,warning:0,advice:0},issues:[]},counts=report.counts||{},issues=report.issues||[],important=Number(counts.error||0)+Number(counts.warning||0);if(actionCount)actionCount.textContent=String(important);if(tabCount)tabCount.textContent=String(issues.length);const summary=document.createElement('header');summary.className='sensecms-builder-quality-summary '+(report.ready?'is-ready':'is-blocked');summary.innerHTML='<div class="sensecms-builder-quality-score"><b>'+Number(report.score||0)+'</b><span>/100</span></div><div><small>PRE-PUBLICATION CHECK</small><strong>'+(report.ready?'No blocking issues':'Resolve blocking issues')+'</strong><span>'+Number(report.checked_sections||blocks.length)+' sections · '+Number(report.checked_languages||languages.length)+' languages</span></div>'+(qualityLoading?'<i class="sensecms-builder-quality-spinner" data-lucide="loader-circle"></i>':'<i data-lucide="'+(report.ready?'shield-check':'shield-alert')+'"></i>');const counters=document.createElement('div');counters.className='sensecms-builder-quality-counters';[['error','Errors','circle-x'],['warning','Warnings','triangle-alert'],['advice','Advice','lightbulb']].forEach(([severity,label,glyph])=>{const item=document.createElement('span');item.className='is-'+severity;item.append(icon(glyph));const copy=document.createElement('b'),small=document.createElement('small');copy.textContent=String(counts[severity]||0);small.textContent=label;item.append(copy,small);counters.append(item)});const list=document.createElement('div');list.className='sensecms-builder-quality-list';if(!issues.length)list.innerHTML='<div class="sensecms-builder-quality-empty"><i data-lucide="badge-check"></i><strong>This draft passes every current Core check</strong><span>Review the live canvas once more, then save or publish with confidence.</span></div>';else issues.forEach(issue=>{const row=document.createElement('button');row.type='button';row.className='sensecms-builder-quality-issue is-'+issue.severity;const tile=document.createElement('i');tile.append(icon(issue.severity==='error'?'circle-x':issue.severity==='warning'?'triangle-alert':'lightbulb'));const copy=document.createElement('span'),category=document.createElement('small'),message=document.createElement('strong'),meta=document.createElement('em');category.textContent=issue.category||'Quality';message.textContent=issue.message||'Review this item.';meta.textContent=[issue.locale?issue.locale.toUpperCase():'',issue.field||'',issue.block_uid?'Open section':issue.target?'Open settings':''].filter(Boolean).join(' · ');copy.append(category,message);if(meta.textContent)copy.append(meta);row.append(tile,copy);if(issue.block_uid||issue.target){row.append(icon('arrow-up-right'));row.onclick=()=>qualityTarget(issue)}else row.disabled=true;list.append(row)});qualityBox.replaceChildren(summary,counters,list);window.lucide?.createIcons()}
 function updateAiTarget(){if(!aiBox)return;const form=aiBox.querySelector('[data-builder-ai-form]');if(!form)return;const block=blocks.find(item=>item.uid===inspectorUid),target=aiBox.querySelector('[data-builder-ai-target]');target.textContent=block?blockTitle(block)+' · '+catalog[block.type].label:'No section selected';const locale=form.elements.locale;if(locale&&languages.some(item=>item.locale===activeLocale))locale.value=activeLocale;updateAiScopes()}
 function updateAiScopes(){if(!aiBox)return;const form=aiBox.querySelector('[data-builder-ai-form]');if(!form)return;const option=form.elements.action.selectedOptions[0],allowed=String(option?.dataset.scopes||'page').split(','),scope=form.elements.scope;[...scope.options].forEach(item=>item.disabled=!allowed.includes(item.value));if(!allowed.includes(scope.value))scope.value=allowed[0]||'page';if(scope.value==='section'&&!blocks.some(item=>item.uid===inspectorUid)&&allowed.includes('page'))scope.value='page'}
 function renderAiProposal(){if(!aiBox)return;const target=aiBox.querySelector('[data-builder-ai-result]');if(!target)return;if(!aiProposal){target.hidden=true;target.replaceChildren();return}const diff=aiProposal.diff||{},summary=diff.summary||{},head=document.createElement('header'),headIcon=icon('sparkles'),copy=document.createElement('div'),overline=document.createElement('small'),title=document.createElement('strong'),meta=document.createElement('span');overline.textContent='AI PROPOSAL · '+String(aiProposal.provider?.name||'Provider');title.textContent=String(aiProposal.summary||'Proposal ready for review.');meta.textContent=String(aiProposal.provider?.model||'')+' · '+String(aiProposal.locale||'').toUpperCase();copy.append(overline,title,meta);head.append(headIcon,copy);const stats=document.createElement('div');stats.className='sensecms-builder-ai-stats';[['added','Added'],['removed','Removed'],['changed','Changed'],['moved','Moved']].forEach(([key,label])=>{const item=document.createElement('span'),count=document.createElement('b'),caption=document.createElement('small');count.textContent=String(Number(summary[key]||0));caption.textContent=label;item.append(count,caption);stats.append(item)});const note=document.createElement('p'),noteCopy=document.createElement('span'),noteTitle=document.createElement('strong'),noteText=document.createElement('small');noteTitle.textContent='Nothing has been saved';noteText.textContent='Apply this proposal to the local draft, inspect it in Live Canvas, then save manually.';noteCopy.append(noteTitle,noteText);note.append(icon('info'),noteCopy);const actions=document.createElement('footer'),discard=document.createElement('button'),apply=document.createElement('button');discard.type=apply.type='button';discard.className='btn bg-default-150';apply.className='btn bg-primary text-white';discard.append(icon('x'),document.createTextNode('Discard'));apply.append(icon('check'),document.createTextNode('Apply to draft'));discard.onclick=()=>{aiProposal=null;aiBaseFingerprint='';renderAiProposal()};apply.onclick=()=>{if(fingerprint()!==aiBaseFingerprint){toast('warning','The page changed after this proposal was generated. Generate a fresh proposal.');return}blocks=(aiProposal.blocks||[]).filter(block=>catalog[block.type]).map(normalize);open=new Set(blocks[0]?[blocks[0].uid]:[]);inspectorUid=blocks[0]?.uid||'';aiProposal=null;aiBaseFingerprint='';render();setDirty(true);switchLibraryTab('ai');toast('success','AI proposal applied to the unsaved draft. Review it before saving.')};actions.append(discard,apply);target.hidden=false;target.replaceChildren(head,stats,note,actions);window.lucide?.createIcons()}
 async function runAiProposal(form){if(!canAi||aiAbort)return;updateAiScopes();const data=new FormData(form),scope=String(data.get('scope')||'page');if(scope==='section'&&!blocks.some(item=>item.uid===inspectorUid)){toast('warning','Select a section before using a section-scoped AI action.');return}const run=form.querySelector('[data-builder-ai-run]'),cancel=form.querySelector('[data-builder-ai-cancel]'),result=aiBox.querySelector('[data-builder-ai-result]');aiBaseFingerprint=fingerprint();aiProposal=null;run.hidden=true;cancel.hidden=false;result.hidden=false;const loading=document.createElement('div');loading.className='sensecms-builder-ai-loading';const loadingTitle=document.createElement('strong'),loadingText=document.createElement('span');loadingTitle.textContent='Preparing a controlled proposal';loadingText.textContent='The provider receives only this selected Builder context.';loading.append(icon('loader-circle'),loadingTitle,loadingText);result.replaceChildren(loading);window.lucide?.createIcons();aiAbort=new AbortController();cancel.onclick=()=>aiAbort?.abort();try{const response=await request('/content/builder/'+payload.page.id+'/ai',{method:'POST',signal:aiAbort.signal,headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,provider:data.get('provider'),action:data.get('action'),scope,locale:data.get('locale'),tone:data.get('tone'),audience:data.get('audience'),instructions:data.get('instructions'),section_uid:inspectorUid,blocks:blockPayload()})});aiProposal=response.data;renderAiProposal()}catch(error){if(error.name==='AbortError'){result.hidden=true;toast('warning','AI request cancelled.')}else{const failure=document.createElement('div'),failureTitle=document.createElement('strong'),failureText=document.createElement('span');failure.className='sensecms-builder-ai-error';failureTitle.textContent='Proposal unavailable';failureText.textContent=String(error.message||'Try another configured provider.');failure.append(icon('triangle-alert'),failureTitle,failureText);result.replaceChildren(failure);toast('error',error.message||'The AI proposal could not be completed.');window.lucide?.createIcons()}}finally{aiAbort=null;run.hidden=false;cancel.hidden=true}}
 function workflowFor(uid){return collaboration.workflow?.[uid]||{status:'draft',assigned_user_id:null,assigned_name:''}}
 function commentsFor(uid){return Array.isArray(collaboration.comments?.[uid])?collaboration.comments[uid]:[]}
 function lockFor(uid){return collaboration.locks?.[uid]||null}
 function lockedByOther(uid){const lock=lockFor(uid);return!!lock&&!lock.is_mine}
 function workflowLabel(status){return status==='approved'?'Approved':status==='in_review'?'In review':'Draft'}
 function applyCollaboration(data){collaboration=data||collaboration;const count=root.querySelector('[data-builder-review-count]');if(count)count.textContent=String(collaboration.open_comments||0);renderReview()}
 async function syncCollaboration(quiet=true){try{const result=await request('/api/content/builder/'+payload.page.id+'/collaboration');applyCollaboration(result.data)}catch(error){if(!quiet)toast('error',error.message||'Collaboration could not be refreshed.')}finally{clearTimeout(collaborationTimer);collaborationTimer=setTimeout(()=>syncCollaboration(true),20000)}}
 async function releaseSection(uid=lockedUid){if(!canEdit||!uid)return;lockedUid=lockedUid===uid?'':lockedUid;clearInterval(lockTimer);try{const result=await request('/content/builder/'+payload.page.id+'/sections/'+encodeURIComponent(uid)+'/unlock',{method:'POST',keepalive:true,headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf})});applyCollaboration(result.data)}catch(error){}}
 async function reserveSection(uid){if(!canEdit||!uid)return true;if(lockedUid===uid){return true}const previous=lockedUid;if(previous)await releaseSection(previous);try{const result=await request('/content/builder/'+payload.page.id+'/sections/'+encodeURIComponent(uid)+'/lock',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf})});lockedUid=uid;applyCollaboration(result.data);clearInterval(lockTimer);lockTimer=setInterval(async()=>{if(lockedUid!==uid)return;try{const refreshed=await request('/content/builder/'+payload.page.id+'/sections/'+encodeURIComponent(uid)+'/lock',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf})});applyCollaboration(refreshed.data)}catch(error){lockedUid='';clearInterval(lockTimer);syncCollaboration()}},30000);return true}catch(error){lockedUid='';await syncCollaboration(true);toast('warning',error.message||'This section is being edited by another person.');return false}}
 async function saveSectionWorkflow(uid,status,assigned){try{const result=await request('/content/builder/'+payload.page.id+'/sections/'+encodeURIComponent(uid)+'/workflow',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,status,assigned_user_id:assigned||null})});applyCollaboration(result.data);render();switchLibraryTab('review');toast('success',result.message)}catch(error){toast('error',error.message);renderReview()}}
 async function postSectionComment(uid,body,button){button.disabled=true;try{const result=await request('/content/builder/'+payload.page.id+'/sections/'+encodeURIComponent(uid)+'/comments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,body})});applyCollaboration(result.data);toast('success',result.message);return true}catch(error){toast('error',error.message);return false}finally{button.disabled=false}}
 async function toggleSectionComment(comment,resolved,button){button.disabled=true;try{const result=await request('/content/builder/'+payload.page.id+'/comments/'+comment.id+'/resolve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,resolved})});applyCollaboration(result.data);toast('success',result.message)}catch(error){toast('error',error.message)}finally{button.disabled=false}}
 function renderReview(){if(!reviewBox)return;const block=blocks.find(item=>item.uid===inspectorUid);if(!block){reviewBox.innerHTML='<div class="sensecms-builder-global-empty"><i data-lucide="messages-square"></i><strong>Select a section to review</strong><span>Comments, assignment and approval status are stored independently for every section.</span></div>';window.lucide?.createIcons();return}const state=workflowFor(block.uid),comments=commentsFor(block.uid),lock=lockFor(block.uid),head=document.createElement('header');head.className='sensecms-builder-review-head';const tile=document.createElement('i');tile.append(icon(catalog[block.type].icon));const copy=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('span');name.textContent=blockTitle(block);meta.textContent=catalog[block.type].label+' · '+comments.filter(item=>!item.resolved).length+' open comments';copy.append(name,meta);head.append(tile,copy);if(lock){const lockBadge=document.createElement('em');lockBadge.className=lock.is_mine?'is-mine':'is-locked';lockBadge.append(icon(lock.is_mine?'pencil-line':'lock-keyhole'),document.createTextNode(lock.is_mine?' Editing now':lock.user_name+' editing'));head.append(lockBadge)}const workflow=document.createElement('section');workflow.className='sensecms-builder-review-workflow';const workflowTitle=document.createElement('header');workflowTitle.innerHTML='<strong>Section workflow</strong><span>Independent from page publication</span>';const controls=document.createElement('div'),statusLabel=document.createElement('label'),statusCaption=document.createElement('span'),status=document.createElement('select');statusCaption.textContent='Status';[['draft','Draft'],['in_review','In review'],['approved','Approved']].forEach(([value,label])=>{const option=document.createElement('option');option.value=value;option.textContent=label;if(value==='approved'&&!canReview)option.disabled=true;status.append(option)});status.value=state.status||'draft';statusLabel.append(statusCaption,status);const assignLabel=document.createElement('label'),assignCaption=document.createElement('span'),assign=document.createElement('select'),unassigned=document.createElement('option');assignCaption.textContent='Assigned to';unassigned.value='';unassigned.textContent='Unassigned';assign.append(unassigned);collaborators.forEach(user=>{const option=document.createElement('option');option.value=String(user.id);option.textContent=user.name+(user.job_title?' · '+user.job_title:'');assign.append(option)});assign.value=state.assigned_user_id?String(state.assigned_user_id):'';assignLabel.append(assignCaption,assign);const update=()=>saveSectionWorkflow(block.uid,status.value,Number(assign.value)||null);status.onchange=update;assign.onchange=update;controls.append(statusLabel,assignLabel);workflow.append(workflowTitle,controls);const thread=document.createElement('section');thread.className='sensecms-builder-review-thread';const threadTitle=document.createElement('header'),threadName=document.createElement('strong'),threadCount=document.createElement('span');threadName.textContent='Discussion';threadCount.textContent=comments.length+' '+(comments.length===1?'comment':'comments');threadTitle.append(threadName,threadCount);const list=document.createElement('div');list.className='sensecms-builder-review-comments';if(!comments.length){const blank=document.createElement('div');blank.className='sensecms-builder-review-empty';blank.append(icon('message-circle'));const blankText=document.createElement('span'),blankTitle=document.createElement('strong'),blankMeta=document.createElement('small');blankTitle.textContent='No comments yet';blankMeta.textContent='Start a focused discussion about this section.';blankText.append(blankTitle,blankMeta);blank.append(blankText);list.append(blank)}else comments.forEach(comment=>{const row=document.createElement('article');row.className='sensecms-builder-review-comment'+(comment.resolved?' is-resolved':'');const commentHead=document.createElement('header'),author=document.createElement('strong'),time=document.createElement('time'),body=document.createElement('p'),action=document.createElement('button');author.textContent=comment.author+(comment.is_mine?' · You':'');time.textContent=new Date(String(comment.created_at).replace(' ','T')).toLocaleString();commentHead.append(author,time);body.textContent=comment.body;action.type='button';action.append(icon(comment.resolved?'rotate-ccw':'check'),document.createTextNode(comment.resolved?' Reopen':' Resolve'));action.onclick=()=>toggleSectionComment(comment,!comment.resolved,action);row.append(commentHead,body,action);list.append(row)});const form=document.createElement('form'),textarea=document.createElement('textarea'),submit=document.createElement('button');textarea.className='form-input';textarea.rows=3;textarea.maxLength=2000;textarea.placeholder='Add a precise, actionable comment…';submit.type='submit';submit.className='btn bg-primary text-white';submit.append(icon('send'),document.createTextNode(' Post comment'));form.append(textarea,submit);form.onsubmit=event=>{event.preventDefault();const body=textarea.value.trim();if(!body){textarea.focus();return}postSectionComment(block.uid,body,submit).then(saved=>{if(saved)textarea.value='';else textarea.focus()})};thread.append(threadTitle,list,form);reviewBox.replaceChildren(head,workflow,thread);window.lucide?.createIcons()}
 function visibleField(field,data){return !field.show_when||Object.entries(field.show_when).every(([key,value])=>String(data[key]??'')===String(value))}
 function contentChoices(field,data){if(field.options_source)return Array.isArray(collectionOptions[field.options_source])?collectionOptions[field.options_source]:[];const source=String(data[field.options_source_field]||'');return Array.isArray(collectionOptions.sources?.[source]?.items)?collectionOptions.sources[source].items:[]}
 function contentMultiselectNode(field,data,onChange){const label=document.createElement('section');label.className='sensecms-builder-field sensecms-builder-content-picker'+(field.wide?' is-wide':'');const caption=document.createElement('span'),count=document.createElement('small');caption.textContent=field.label;caption.append(count);const toolbar=document.createElement('div');toolbar.className='sensecms-builder-content-picker-toolbar';const search=document.createElement('input');search.type='search';search.className='form-input';search.placeholder='Search published content…';toolbar.append(search);const list=document.createElement('div');list.className='sensecms-builder-content-picker-list';const selected=()=>Array.isArray(data[field.key])?data[field.key].map(Number).filter(value=>value>0):[];data[field.key]=selected();const paint=()=>{const values=selected(),chosen=new Set(values),query=search.value.trim().toLocaleLowerCase(),choices=contentChoices(field,data).map(item=>({...item,value:Number(item.value)}));values.filter(value=>!choices.some(item=>item.value===value)).forEach(value=>choices.push({value,label:'Unavailable item #'+value,meta:'Remove or republish this content'}));const filtered=choices.filter(item=>!query||`${item.label||''} ${item.meta||''}`.toLocaleLowerCase().includes(query));count.textContent=values.length+' / '+field.max+' selected';list.replaceChildren(...filtered.map(item=>{const row=document.createElement('label'),input=document.createElement('input'),copy=document.createElement('span'),name=document.createElement('strong'),meta=document.createElement('small');input.type='checkbox';input.checked=chosen.has(item.value);input.disabled=!input.checked&&values.length>=field.max;name.textContent=String(item.label||('Item #'+item.value));meta.textContent=String(item.meta||'Published content');copy.append(name,meta);row.append(input,copy);input.onchange=()=>{const current=selected(),index=current.indexOf(item.value);if(input.checked&&index<0&&current.length<field.max)current.push(item.value);else if(!input.checked&&index>=0)current.splice(index,1);data[field.key]=current;onChange(false);paint()};return row}));if(!filtered.length){const empty=document.createElement('p');empty.textContent=choices.length?'No matching published content.':'No published content is available for this source.';list.append(empty)}};search.oninput=paint;label.append(caption,toolbar,list);paint();return label}
 function fieldNode(field,data,onChange){
   if(field.type==='repeater')return repeaterNode(field,data,onChange);if(field.type==='content-multiselect')return contentMultiselectNode(field,data,onChange);
   const label=document.createElement('label');label.className='sensecms-builder-field'+(field.wide?' is-wide':'');const caption=document.createElement('span');caption.textContent=field.label;if(field.required){const required=document.createElement('small');required.textContent='Required';caption.append(required)}label.append(caption);
   let control;if(field.type==='select'||field.type==='content-select'){control=document.createElement('select');if(field.type==='select')Object.entries(field.options||{}).forEach(([value,text])=>{const option=document.createElement('option');option.value=value;option.textContent=text;control.append(option)});else{const empty=document.createElement('option');empty.value='';empty.textContent=field.empty_label||'Any';control.append(empty);const choices=contentChoices(field,data);choices.forEach(item=>{const option=document.createElement('option');option.value=String(item.value);option.textContent=String(item.label)+(item.meta?' · '+item.meta:'');control.append(option)});const current=String(data[field.key]??'');if(current&&!choices.some(item=>String(item.value)===current)){const missing=document.createElement('option');missing.value=current;missing.textContent='Unavailable item #'+current;control.append(missing)}}control.value=String(data[field.key]??'')}else if(field.type==='checkbox'){control=document.createElement('input');control.type='checkbox';control.checked=!!data[field.key];label.classList.add('is-checkbox')}else{control=field.type==='textarea'||field.type==='code'?document.createElement('textarea'):document.createElement('input');control.value=String(data[field.key]??'');if(field.type==='textarea'||field.type==='code'){control.rows=field.type==='code'?10:4;if(field.type==='code')control.classList.add('is-code')}else control.type=field.type==='number'?'number':field.type==='email'?'email':'text';if(field.min!==undefined)control.min=field.min;if(field.max!==undefined&&field.type==='number')control.max=field.max;else control.maxLength=field.max||500}control.classList.add(field.type==='select'||field.type==='content-select'?'form-select':'form-input');control.required=!!field.required;control.addEventListener(field.type==='checkbox'?'change':'input',()=>{data[field.key]=field.type==='checkbox'?control.checked:field.type==='number'?Number(control.value):control.value;if(field.key==='source'&&Array.isArray(data.selected_ids))data.selected_ids=[];onChange(field.type==='select'||field.type==='content-select')});
   if(['image','video'].includes(field.type)){const media=document.createElement('div');media.className='sensecms-builder-media';media.append(control);const choose=document.createElement('button'),upload=document.createElement('button');choose.type=upload.type='button';choose.textContent='Choose';upload.textContent='Upload '+field.type;choose.addEventListener('click',()=>openMedia(field.type,control,label,onChange));upload.addEventListener('click',()=>uploadMedia(field.type,control,label,onChange));media.append(choose,upload);label.append(media);renderMediaPreview(label,control.value,field.type)}else label.append(control);return label;
 }
 function renderMediaPreview(label,url,kind){label.querySelector('.sensecms-builder-media-preview')?.remove();if(!url)return;const preview=document.createElement('div');preview.className='sensecms-builder-media-preview';const media=document.createElement(kind==='video'?'video':'img');media.src=url;if(kind==='video'){media.muted=true;media.preload='metadata'}const text=document.createElement('span');text.textContent=url;preview.append(media,text);label.append(preview)}
 function uploadMedia(kind,control,label,onChange){const input=document.createElement('input');input.type='file';input.accept=kind==='video'?'video/mp4':'image/jpeg,image/png,image/webp';input.onchange=async()=>{const file=input.files?.[0];if(!file)return;const form=new FormData();form.set('csrf',payload.csrf);form.set('page_id',String(payload.page.id));form.set('kind',kind);form.set('asset',file);label.classList.add('sensecms-builder-uploading');try{const response=await fetch('/content/builder/media',{method:'POST',body:form,headers:{Accept:'application/json','X-SenseCMS-Request':'1'}}),result=await response.json();if(!response.ok||!result.ok)throw new Error(result.message||'Upload failed.');control.value=result.data.url;control.dispatchEvent(new Event('input',{bubbles:true}));renderMediaPreview(label,result.data.url,kind);toast('success',result.message)}catch(error){toast('error',error.message||'Upload failed.')}finally{label.classList.remove('sensecms-builder-uploading')}};input.click()}
 function openMedia(kind,control,label,onChange){mediaTarget={kind,control,label,onChange};mediaPage=1;root.querySelector('[data-media-kind]').value=kind;openDialog(root.querySelector('[data-builder-media-dialog]'));loadMedia()}
 async function loadMedia(){const dialog=root.querySelector('[data-builder-media-dialog]'),grid=dialog.querySelector('[data-media-grid]'),pagination=dialog.querySelector('[data-media-pagination]'),query=dialog.querySelector('[data-media-search]').value.trim(),kind=dialog.querySelector('[data-media-kind]').value,status=message=>{const empty=document.createElement('div');empty.className='sensecms-builder-revisions-empty';empty.textContent=message;grid.replaceChildren(empty)};status('Loading media…');try{const result=await request('/api/content/builder/media?q='+encodeURIComponent(query)+'&kind='+encodeURIComponent(kind)+'&page='+mediaPage),items=result.data.items||[],pages=Number(result.data.pagination?.pages||1);grid.replaceChildren(...items.map(item=>{const card=document.createElement('button');card.type='button';card.className='sensecms-builder-media-card';const figure=document.createElement('figure'),asset=document.createElement(String(item.mime_type).startsWith('video/')?'video':'img');asset.src=item.url;if(asset.tagName==='VIDEO'){asset.muted=true;asset.preload='metadata'}figure.append(asset);const copy=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('span');name.textContent=item.original_name;meta.textContent=[item.width&&item.height?item.width+' × '+item.height:null,Math.max(1,Math.round(Number(item.size_bytes)/1024))+' KB'].filter(Boolean).join(' · ');copy.append(name,meta);card.append(figure,copy);card.onclick=()=>{if(!mediaTarget)return;mediaTarget.control.value=item.url;mediaTarget.control.dispatchEvent(new Event('input',{bubbles:true}));renderMediaPreview(mediaTarget.label,item.url,mediaTarget.kind);closeDialog(dialog)};return card}));if(!items.length)status('No matching media found.');const start=Math.max(1,Math.min(mediaPage-2,pages-4)),end=Math.min(pages,start+4);pagination.replaceChildren(...Array.from({length:end-start+1},(_,index)=>{const page=start+index,control=document.createElement('button');control.type='button';control.textContent=page;control.className=page===mediaPage?'is-active':'';control.onclick=()=>{mediaPage=page;loadMedia()};return control}))}catch(error){status(error.message||'Media could not be loaded.');pagination.replaceChildren()}}
  function repeaterNode(field,data,onChange){const root=document.createElement('section');root.className='sensecms-builder-repeater';const head=document.createElement('header'),title=document.createElement('strong'),add=document.createElement('button');title.textContent=field.label;add.type='button';add.textContent='+ Add '+field.item_label.toLowerCase();head.append(title,add);const box=document.createElement('div');box.className='sensecms-builder-repeater-list';root.append(head,box);let items=Array.isArray(data[field.key])?data[field.key]:[];data[field.key]=items;
   const paint=()=>{box.replaceChildren();items.forEach((item,index)=>{const card=document.createElement('article');card.className='sensecms-builder-repeater-item';const itemHead=document.createElement('header'),name=document.createElement('strong'),remove=document.createElement('button');name.textContent=field.item_label+' '+(index+1);remove.type='button';remove.append(icon('trash-2'));remove.disabled=items.length<=field.min;remove.onclick=()=>{items.splice(index,1);onChange();paint()};itemHead.append(name,remove);const fields=document.createElement('div');fields.className='sensecms-builder-fields';field.fields.filter(child=>visibleField(child,item)).forEach(child=>fields.append(fieldNode(child,item,onChange)));card.append(itemHead,fields);box.append(card)});window.lucide?.createIcons()};
    add.onclick=()=>{if(items.length>=field.max){toast('warning','This section allows up to '+field.max+' items.');return}const item={};field.fields.forEach(child=>item[child.key]=child.type==='repeater'?[]:child.type==='checkbox'?false:child.type==='number'?(child.min||0):child.type==='select'?Object.keys(child.options||{})[0]||'':'');items.push(item);onChange();paint()};paint();return root;
  }
  function setLiveStatus(message,state=''){const target=root.querySelector('[data-builder-live-state]');if(!target)return;target.textContent=message;target.closest('span')?.classList.toggle('is-updating',state==='is-updating');target.closest('span')?.classList.toggle('is-error',state==='is-error')}
  function highlightLive(scroll=false){if(!liveFrame?.contentWindow)return;liveFrame.contentWindow.postMessage({type:'sensecms-builder-highlight',uid:inspectorUid,scroll},location.origin)}
  function refreshLivePreview(){if(!liveMode||!liveFrame)return;const url=new URL(payload.page.live_preview_url,location.origin);url.searchParams.set('locale',activeLocale);url.searchParams.set('_',String(Date.now()));setLiveStatus('Rendering private draft…','is-updating');liveFrame.src=url.pathname+url.search;previewFingerprint=fingerprint()}
  function switchLibraryTab(name){const tab=root.querySelector('[data-library-tab="'+name+'"]');if(!tab)return;root.querySelectorAll('[data-library-tab]').forEach(item=>item.classList.toggle('is-active',item===tab));root.querySelectorAll('[data-library-panel]').forEach(panel=>panel.hidden=panel.dataset.libraryPanel!==name);if(name==='quality')loadQuality();if(name==='review')renderReview();if(name==='ai')updateAiTarget()}
  function selectCanvasBlock(uid,scroll=false){const block=blocks.find(item=>item.uid===uid);if(!block)return;inspectorUid=uid;open.add(uid);renderInspector();renderReview();updateAiTarget();switchLibraryTab(canEdit?'inspector':'review');if(canEdit)reserveSection(uid).then(ok=>{if(!ok){renderInspector();render()}});highlightLive(scroll);window.lucide?.createIcons()}
  function removeBlock(block){const index=blocks.indexOf(block);if(index<0)return;window.SenseCMSUI?.modal('Archive section','This removes the section from the page after you save. Its previous database record remains archived for data safety.',()=>{const group=block.layout.group;blocks.splice(index,1);repairGroup(group);open.delete(block.uid);selected.delete(block.uid);if(inspectorUid===block.uid)inspectorUid=blocks[Math.min(index,blocks.length-1)]?.uid||'';setDirty();render();highlightLive()})}
  function runLiveAction(uid,action){const block=blocks.find(item=>item.uid===uid),index=block?blocks.indexOf(block):-1;if(!block)return;if(action==='settings'){selectCanvasBlock(uid);return}if(!canEdit||lockedByOther(uid)){switchLibraryTab('review');toast('warning',lockedByOther(uid)?lockFor(uid).user_name+' is editing this section.':'Review mode does not change section content.');return}if(action==='layout'){openLayout(block);return}if(action==='up'&&index>0){move(index,-1);selectCanvasBlock(uid);return}if(action==='down'&&index<blocks.length-1){move(index,1);selectCanvasBlock(uid);return}if(action==='visibility'){block.visible=!block.visible;setDirty();render();selectCanvasBlock(uid);return}if(action==='duplicate'){duplicateBlock(block,index);return}if(action==='remove')removeBlock(block)}
  function appearanceGroup(block){const group=document.createElement('section'),title=document.createElement('header'),fields=document.createElement('div'),reset=document.createElement('button');group.className='sensecms-builder-inspector-group sensecms-builder-appearance';title.innerHTML='<strong>Appearance</strong><span>Portable Core styles</span>';fields.className='sensecms-builder-appearance-fields';Object.entries(appearances).forEach(([key,definition])=>{const label=document.createElement('label'),caption=document.createElement('span'),select=document.createElement('select');caption.textContent=definition.label||key;Object.entries(definition.options||{}).forEach(([value,text])=>{const option=document.createElement('option');option.value=value;option.textContent=text;select.append(option)});select.value=block.appearance[key]||'default';select.onchange=()=>{block.appearance[key]=select.value;setDirty();renderInspector()};label.append(caption,select);fields.append(label)});reset.type='button';reset.className='sensecms-builder-appearance-reset';reset.append(icon('rotate-ccw'),document.createTextNode('Use theme defaults'));reset.onclick=()=>{block.appearance=defaultAppearance();setDirty();renderInspector()};group.append(title,fields,reset);return group}
  function renderInspector(){if(!inspector)return;const block=blocks.find(item=>item.uid===inspectorUid);if(!block){inspector.innerHTML='<div class="sensecms-builder-global-empty"><i data-lucide="mouse-pointer-click"></i><strong>Select a section on the canvas</strong><span>Its content, visibility and responsive settings will appear here.</span></div>';return}const editable=canEdit&&!lockedByOther(block.uid),definition=catalog[block.type],head=document.createElement('header');head.className='sensecms-builder-inspector-head';const tile=document.createElement('i');tile.append(icon(definition.icon));const copy=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('span');name.textContent=blockTitle(block);meta.textContent=definition.label+' · '+coverage(block)+'% content';copy.append(name,meta);head.append(tile,copy);const actions=document.createElement('div');actions.className='sensecms-builder-inspector-actions';[['columns-3','layout','Responsive settings'],['copy-plus','duplicate','Duplicate section'],[block.visible?'eye':'eye-off','visibility',block.visible?'Hide section':'Show section'],['trash-2','remove','Remove section']].forEach(([glyph,action,title])=>{const control=button(glyph,action,title);control.disabled=!editable;if(action==='remove')control.classList.add('is-danger');control.onclick=()=>runLiveAction(block.uid,action);actions.append(control)});head.append(actions);const body=document.createElement('div');body.className='sensecms-builder-inspector-body';if(!editable){const notice=document.createElement('div');notice.className='sensecms-builder-edit-lock';notice.append(icon(lockedByOther(block.uid)?'lock-keyhole':'shield-check'));const noticeCopy=document.createElement('span'),noticeTitle=document.createElement('strong'),noticeText=document.createElement('small');noticeTitle.textContent=lockedByOther(block.uid)?lockFor(block.uid).user_name+' is editing':'Review mode';noticeText.textContent=lockedByOther(block.uid)?'Content controls unlock automatically when that editing session ends.':'Use the Review tab to comment, assign and approve this section.';noticeCopy.append(noticeTitle,noticeText);notice.append(noticeCopy);body.append(notice)}const visibility=document.createElement('label');visibility.className='sensecms-builder-inspector-toggle';const toggle=document.createElement('input');toggle.type='checkbox';toggle.checked=block.visible;toggle.disabled=!editable;const toggleCopy=document.createElement('span');toggleCopy.innerHTML='<b>Visible on website</b><small>Hidden sections remain available as a faded canvas item.</small>';toggle.onchange=()=>{block.visible=toggle.checked;setDirty();render();selectCanvasBlock(block.uid)};visibility.append(toggle,toggleCopy);body.append(visibility,appearanceGroup(block));if(definition.shared_fields?.length){const group=document.createElement('section'),title=document.createElement('header'),fields=document.createElement('div');group.className='sensecms-builder-inspector-group';title.innerHTML='<strong>Section settings</strong><span>Shared by every language</span>';fields.className='sensecms-builder-fields';definition.shared_fields.filter(field=>visibleField(field,block.shared)).forEach(field=>fields.append(fieldNode(field,block.shared,(refresh=true)=>{setDirty();if(refresh)renderInspector()})));group.append(title,fields);body.append(group)}if(definition.fields.length){const group=document.createElement('section'),title=document.createElement('header'),fields=document.createElement('div'),language=languages.find(item=>item.locale===activeLocale),localeName=document.createElement('strong'),localeMeta=document.createElement('span');group.className='sensecms-builder-inspector-group';localeName.textContent=language?.native_name||activeLocale.toUpperCase();localeMeta.textContent='Localized content';title.append(localeName,localeMeta);fields.className='sensecms-builder-fields';const data=block.localized[activeLocale];definition.fields.filter(field=>visibleField(field,data)).forEach(field=>fields.append(fieldNode(field,data,(refresh=true)=>{setDirty();if(refresh)renderInspector()})));group.append(title,fields);body.append(group)}const footer=document.createElement('footer'),responsive=document.createElement('button');responsive.type='button';responsive.className='btn bg-default-150';responsive.disabled=!editable;responsive.append(icon('monitor-smartphone'),document.createTextNode('Responsive layout'));responsive.onclick=()=>openLayout(block);footer.append(responsive);body.append(footer);if(!editable)body.querySelectorAll('input,textarea,select,button').forEach(control=>control.disabled=true);inspector.replaceChildren(head,body);window.lucide?.createIcons()}
  function switchBuilderView(mode){liveMode=mode==='live';root.querySelector('[data-builder-structure]').hidden=liveMode;root.querySelector('[data-builder-live]').hidden=!liveMode;root.querySelectorAll('[data-builder-view]').forEach(control=>control.classList.toggle('is-active',control.dataset.builderView===mode));root.querySelector('[data-builder-canvas-help]').textContent=liveMode?'Select sections directly on the page. Edit them in Inspector and preview each responsive width.':'Drag sections into the required order. Every section has independent content for each active language.';if(!liveMode)return;if(!inspectorUid&&blocks[0])inspectorUid=blocks[0].uid;renderInspector();switchLibraryTab('inspector');if(dirty)flushDraft({quiet:true});else refreshLivePreview()}
 function blockNode(block,index){const editable=canEdit&&!lockedByOther(block.uid),definition=catalog[block.type],article=document.createElement('article');article.className='sensecms-builder-block'+(open.has(block.uid)?' is-open':'')+(block.visible?'':' is-hidden')+(editable?'':' is-edit-locked');article.dataset.uid=block.uid;article.style.setProperty('--block-accent',index%3===0?'#2f7df6':index%3===1?'#c92635':'#0f9f67');
   const head=document.createElement('header');head.className='sensecms-builder-block-head';const drag=button('grip-vertical','drag','Drag to reorder');drag.className='sensecms-builder-drag';drag.disabled=!editable;drag.draggable=editable;drag.addEventListener('dragstart',event=>{if(!editable)return;dragUid=block.uid;article.classList.add('is-dragging');event.dataTransfer.effectAllowed='move';event.dataTransfer.setData('text/plain',block.uid)});drag.addEventListener('dragend',()=>{dragUid='';article.classList.remove('is-dragging');list.querySelectorAll('.is-drop-target').forEach(item=>item.classList.remove('is-drop-target'))});
    const tile=document.createElement('span');tile.className='sensecms-builder-block-icon';tile.append(icon(definition.icon));const title=document.createElement('div');title.className='sensecms-builder-block-title';const strong=document.createElement('strong');strong.textContent=definition.label;if(definition.singleton){const badge=document.createElement('em');badge.textContent='Single';strong.append(badge)}const description=document.createElement('span');description.textContent=blockTitle(block)+' · '+coverage(block)+'% content';title.append(strong,description);const badges=document.createElement('div');badges.className='sensecms-builder-block-badges';const workflow=workflowFor(block.uid),comments=commentsFor(block.uid),lock=lockFor(block.uid),workflowBadge=document.createElement('em');workflowBadge.className='is-workflow is-'+workflow.status;workflowBadge.append(icon(workflow.status==='approved'?'badge-check':workflow.status==='in_review'?'scan-eye':'pencil-line'),document.createTextNode(workflowLabel(workflow.status)));badges.append(workflowBadge);if(comments.length){const badge=document.createElement('em');badge.className='is-comments';badge.append(icon('message-circle'),document.createTextNode(String(comments.filter(item=>!item.resolved).length)));badges.append(badge)}if(lock){const badge=document.createElement('em');badge.className=lock.is_mine?'is-editing':'is-locked';badge.append(icon(lock.is_mine?'pencil-line':'lock-keyhole'),document.createTextNode(lock.is_mine?'Editing':lock.user_name));badges.append(badge)}if(block.global_section_id){const badge=document.createElement('em');badge.append(icon('combine'),document.createTextNode(block.global_name||'Shared section'));badges.append(badge)}if(block.layout.group||block.layout.container!=='content'||block.layout.desktop.span!==12){const badge=document.createElement('em');badge.className='is-layout';badge.append(icon('columns-3'),document.createTextNode((block.layout.group?block.layout.mode:'section')+' · '+block.layout.desktop.span+'/12'));badges.append(badge)}const now=new Date(),from=block.visible_from?new Date(block.visible_from):null,until=block.visible_until?new Date(block.visible_until):null;if(from&&from>now){const badge=document.createElement('em');badge.className='is-scheduled';badge.append(icon('clock-3'),document.createTextNode('Scheduled'));badges.append(badge)}else if(until&&until<=now){const badge=document.createElement('em');badge.className='is-expired';badge.append(icon('calendar-x'),document.createTextNode('Expired'));badges.append(badge)}if(badges.children.length)title.append(badges);
    const controls=document.createElement('div');controls.className='sensecms-builder-block-controls';const toggle=document.createElement('label');toggle.className='sensecms-builder-toggle';toggle.title=block.visible?'Visible on website':'Hidden from website';const checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.checked=block.visible;const slider=document.createElement('span');checkbox.onchange=()=>{block.visible=checkbox.checked;setDirty();render()};toggle.append(checkbox,slider);const layout=button('columns-3','layout','Responsive layout settings'),share=button(block.global_section_id?'unlink':'combine',block.global_section_id?'detach':'share',block.global_section_id?'Detach shared section':'Save as shared section'),copySection=button('clipboard-copy','copy','Copy section to private clipboard'),up=button('arrow-up','up','Move section up'),down=button('arrow-down','down','Move section down'),duplicate=button('copy-plus','duplicate','Duplicate section'),remove=button('trash-2','remove','Archive section');share.className='sensecms-builder-share-action'+(block.global_section_id?' is-linked':'');layout.className='sensecms-builder-layout-action';remove.classList.add('is-danger');const expand=button(open.has(block.uid)?'chevron-up':'chevron-down','expand',open.has(block.uid)?'Collapse section':'Edit section');up.disabled=index===0;down.disabled=index===blocks.length-1;
    [checkbox,layout,share,copySection,up,down,duplicate,remove].forEach(control=>control.disabled=!editable||control.disabled);layout.onclick=()=>openLayout(block);share.onclick=()=>block.global_section_id?window.SenseCMSUI?.modal('Detach shared section','This page will keep an independent copy. Future shared updates will no longer affect it.',()=>{block.global_section_id=null;block.global_name='';setDirty();render()}):openShare(block);copySection.onclick=()=>copyBlocks([block]);up.onclick=()=>move(index,-1);down.onclick=()=>move(index,1);duplicate.onclick=()=>duplicateBlock(block,index);remove.onclick=()=>removeBlock(block);expand.onclick=async()=>{if(!open.has(block.uid)&&canEdit&&!await reserveSection(block.uid))return;open.has(block.uid)?open.delete(block.uid):open.add(block.uid);render()};controls.append(toggle,layout,share,copySection,up,down,duplicate,remove,expand);head.append(drag,tile,title,controls);article.append(head);
   if(open.has(block.uid)){const body=document.createElement('div');body.className='sensecms-builder-block-body';body.addEventListener('focusin',()=>reserveSection(block.uid),{once:true});const schedule=document.createElement('section');schedule.className='sensecms-builder-schedule';schedule.innerHTML='<header><i data-lucide="calendar-clock"></i><div><strong>Visibility schedule</strong><small>Optional publishing window in the website timezone</small></div></header>';const scheduleFields=document.createElement('div');scheduleFields.className='sensecms-builder-schedule-fields';[['visible_from','Visible from'],['visible_until','Visible until']].forEach(([key,label])=>{const field=document.createElement('label'),input=document.createElement('input');field.append(document.createTextNode(label));input.type='datetime-local';input.value=block[key]||'';input.onchange=()=>{block[key]=input.value;if(block.visible_from&&block.visible_until&&block.visible_from>=block.visible_until){toast('warning','The end date must be later than the start date.')}setDirty();render()};field.append(input);scheduleFields.append(field)});schedule.append(scheduleFields);body.append(schedule);if(definition.shared_fields?.length){const shared=document.createElement('section');shared.className='sensecms-builder-shared';const heading=document.createElement('header');heading.innerHTML='<span></span><div><strong>Shared settings</strong><small>Applied to every language'+(block.global_section_id?' and every linked page':'')+'</small></div>';const fields=document.createElement('div');fields.className='sensecms-builder-fields';definition.shared_fields.filter(field=>visibleField(field,block.shared)).forEach(field=>fields.append(fieldNode(field,block.shared,(refresh=true)=>{setDirty();if(refresh)render()})));shared.append(heading,fields);body.append(shared)}if(definition.fields.length){const localeTitle=document.createElement('div');localeTitle.className='sensecms-builder-locale-heading';localeTitle.innerHTML='<strong>'+((languages.find(item=>item.locale===activeLocale)||{}).native_name||activeLocale.toUpperCase())+'</strong><span>Localized content'+(block.global_section_id?' · shared globally':'')+'</span>';const fields=document.createElement('div');fields.className='sensecms-builder-fields';const data=block.localized[activeLocale];definition.fields.filter(field=>visibleField(field,data)).forEach(field=>fields.append(fieldNode(field,data,(refresh=true)=>{setDirty();if(refresh)render()})));body.append(localeTitle,fields)}if(!editable)body.querySelectorAll('input,textarea,select,button').forEach(control=>control.disabled=true);article.append(body)}return article;
 }
 function render(){selected=new Set([...selected].filter(uid=>blocks.some(block=>block.uid===uid)));if(inspectorUid&&!blocks.some(block=>block.uid===inspectorUid))inspectorUid=blocks[0]?.uid||'';list.replaceChildren(...blocks.map(blockNode));empty.hidden=blocks.length>0;count.textContent=String(blocks.length);root.querySelector('[data-builder-outline-count]').textContent=String(blocks.length);renderOutline();renderInspector();renderReview();renderClipboard();renderLibrary();renderPatterns();renderLayouts();renderGlobals();renderQuality();updateAiTarget();renderAiProposal();highlightLive();window.lucide?.createIcons()}
 function renderOutline(){const target=root.querySelector('[data-builder-outline]'),selectedCount=root.querySelector('[data-builder-outline-selected]'),copySelected=root.querySelector('[data-builder-copy-selected]');selectedCount.textContent=String(selected.size);copySelected.disabled=!canEdit||!selected.size;if(!blocks.length){target.innerHTML='<div class="sensecms-builder-global-empty"><i data-lucide="list-tree"></i><strong>No sections on this page</strong><span>Add a section to start building the page outline.</span></div>';window.lucide?.createIcons();return}target.replaceChildren(...blocks.map((block,index)=>{const row=document.createElement('article');row.className='sensecms-builder-outline-item'+(open.has(block.uid)?' is-active':'')+(block.visible?'':' is-hidden');const select=document.createElement('input');select.type='checkbox';select.disabled=!canEdit||lockedByOther(block.uid);select.checked=selected.has(block.uid);select.setAttribute('aria-label','Select '+blockTitle(block));select.onchange=()=>{select.checked?selected.add(block.uid):selected.delete(block.uid);renderOutline();renderLayouts()};const jump=document.createElement('button');jump.type='button';jump.className='sensecms-builder-outline-jump';const tile=document.createElement('i');tile.append(icon(catalog[block.type].icon));const copy=document.createElement('span'),name=document.createElement('strong'),meta=document.createElement('small'),state=workflowFor(block.uid),comments=commentsFor(block.uid).filter(item=>!item.resolved).length,lock=lockFor(block.uid);name.textContent=blockTitle(block);meta.textContent=[catalog[block.type].label,workflowLabel(state.status),state.assigned_name||'',comments?comments+' open':'',lock&&!lock.is_mine?lock.user_name+' editing':''].filter(Boolean).join(' · ');copy.append(name,meta);jump.append(tile,copy);jump.onclick=()=>{inspectorUid=block.uid;open.add(block.uid);render();switchLibraryTab(canEdit?'inspector':'review');if(canEdit)reserveSection(block.uid);setTimeout(()=>list.querySelector('[data-uid="'+CSS.escape(block.uid)+'"]')?.scrollIntoView({behavior:'smooth',block:'start'}),20)};const actions=document.createElement('span'),up=button('chevron-up','outline-up','Move section up'),down=button('chevron-down','outline-down','Move section down');up.disabled=!canEdit||lockedByOther(block.uid)||index===0;down.disabled=!canEdit||lockedByOther(block.uid)||index===blocks.length-1;up.onclick=()=>move(index,-1);down.onclick=()=>move(index,1);actions.append(up,down);row.append(select,jump,actions);return row}));window.lucide?.createIcons()}
 function selectedBlocks(){return blocks.filter(block=>selected.has(block.uid))}
 function applyLayoutPreset(preset){const items=selectedBlocks(),indexes=items.map(item=>blocks.indexOf(item));if(items.length<preset.min||items.length>preset.max){toast('warning','Select '+(preset.min===preset.max?preset.min:preset.min+' to '+preset.max)+' consecutive sections for this layout.');return}if(indexes[indexes.length-1]-indexes[0]+1!==indexes.length){toast('warning','Layout groups require consecutive sections. Reorder or adjust your selection first.');return}const previousGroups=new Set(items.map(block=>block.layout.group).filter(Boolean));for(const oldGroup of previousGroups)blocks.filter(block=>block.layout.group===oldGroup).forEach(block=>block.layout=defaultLayout());const group=items.length>1?uuid():null,count=items.length,even=Math.max(1,Math.floor(12/count));items.forEach((block,index)=>{const span=Number(preset.spans?.[index]??preset.spans?.[0]??even),layout=defaultLayout();layout.group=group;layout.mode=preset.mode;layout.container=preset.container;layout.desktop.span=span;layout.tablet.span=preset.mode==='stack'?12:(count===2?6:Math.max(6,span));layout.mobile.span=12;block.layout=layout});setDirty();render();toast('success',preset.label+' applied to '+items.length+' '+(items.length===1?'section':'sections')+'.')}
 function renderLayouts(){const target=root.querySelector('[data-builder-layouts]'),selectedCount=root.querySelector('[data-builder-layout-selected]');if(!target||!selectedCount)return;selectedCount.textContent=String(selected.size);target.replaceChildren(...layouts.map(preset=>{const card=document.createElement('button');card.type='button';card.className='sensecms-builder-layout-card';const tile=document.createElement('i');tile.append(icon(preset.icon||'columns-3'));const copy=document.createElement('span'),name=document.createElement('strong'),description=document.createElement('small'),requirement=document.createElement('em');name.textContent=preset.label;description.textContent=preset.description;requirement.textContent=preset.min===preset.max?preset.min+' selected':preset.min+'–'+preset.max+' selected';copy.append(name,description,requirement);card.append(tile,copy,icon('arrow-right'));card.onclick=()=>applyLayoutPreset(preset);return card}),...['separator','spacer'].filter(type=>catalog[type]).map(type=>{const item=catalog[type],card=document.createElement('button');card.type='button';card.className='sensecms-builder-layout-card is-structure';const tile=document.createElement('i');tile.append(icon(item.icon));const copy=document.createElement('span'),name=document.createElement('strong'),description=document.createElement('small'),requirement=document.createElement('em');name.textContent=item.label;description.textContent=item.description;requirement.textContent='Add structural section';copy.append(name,description,requirement);card.append(tile,copy,icon('plus'));card.onclick=()=>addBlock(type);return card}));window.lucide?.createIcons()}
 function renderClipboard(){const box=root.querySelector('[data-builder-clipboard]'),paste=root.querySelector('[data-builder-paste]'),items=Array.isArray(clipboard?.blocks)?clipboard.blocks.filter(block=>catalog[block.type]):[];paste.disabled=!items.length;if(!items.length){box.innerHTML='<i data-lucide="clipboard"></i><span><strong>Private clipboard is empty</strong><small>Copy up to 20 sections and paste them on another page.</small></span>';return}const source=String(clipboard.source_title||'another page'),time=clipboard.updated_at?new Date(String(clipboard.updated_at).replace(' ','T')).toLocaleString():'';box.replaceChildren();const mark=icon('clipboard-check'),copy=document.createElement('span'),name=document.createElement('strong'),meta=document.createElement('small'),clear=document.createElement('button');name.textContent=items.length+' copied '+(items.length===1?'section':'sections');meta.textContent='From '+source+(time?' · '+time:'');copy.append(name,meta);clear.type='button';clear.title='Clear private clipboard';clear.setAttribute('aria-label','Clear private clipboard');clear.append(icon('x'));clear.onclick=clearClipboard;box.append(mark,copy,clear)}
 async function copyBlocks(items){if(!items.length)return;try{const result=await request('/content/builder/'+payload.page.id+'/clipboard',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,blocks:items.map(block=>blockPayload().find(item=>item.uid===block.uid)).filter(Boolean)})});clipboard=result.data;selected.clear();render();toast('success',result.message)}catch(error){toast('error',error.message||'The sections could not be copied.')}}
 function pasteClipboard(){const source=Array.isArray(clipboard?.blocks)?clipboard.blocks:[],copies=[],room=Math.max(0,60-blocks.length),groupMap=new Map();let skipped=0;source.forEach(raw=>{if(copies.length>=room||!catalog[raw.type]||(catalog[raw.type].singleton&&blocks.some(block=>block.type===raw.type))){skipped++;return}const copy=normalize(raw),oldGroup=copy.layout.group;copy.uid=uuid();copy.global_section_id=null;copy.global_name='';copy.global_version=0;if(oldGroup){if(!groupMap.has(oldGroup))groupMap.set(oldGroup,uuid());copy.layout.group=groupMap.get(oldGroup)}copies.push(copy)});if(!copies.length){toast('warning',room<1?'A page can contain up to 60 sections.':skipped?'The copied singleton sections already exist on this page.':'The private clipboard has no compatible sections.');return}for(const group of groupMap.values())if(copies.filter(block=>block.layout.group===group).length<2)copies.filter(block=>block.layout.group===group).forEach(block=>{block.layout.group=null;block.layout.mode='stack'});const indexes=[...selected].map(uid=>blocks.findIndex(block=>block.uid===uid)).filter(index=>index>=0),at=indexes.length?Math.max(...indexes)+1:blocks.length;blocks.splice(at,0,...copies);selected.clear();open=new Set(copies.map(block=>block.uid));setDirty();render();setTimeout(()=>list.querySelector('[data-uid="'+CSS.escape(copies[0].uid)+'"]')?.scrollIntoView({behavior:'smooth',block:'center'}),20);toast('success',copies.length+' '+(copies.length===1?'section':'sections')+' pasted'+(skipped?' · '+skipped+' incompatible or over limit skipped':'')+'.')}
 async function clearClipboard(){try{const result=await request('/content/builder/clipboard/clear',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf})});clipboard=null;renderClipboard();window.lucide?.createIcons();toast('success',result.message)}catch(error){toast('error',error.message||'The private clipboard could not be cleared.')}}
 function unitAt(index){const group=blocks[index]?.layout.group;if(!group)return{start:index,end:index,items:blocks.slice(index,index+1)};const indexes=blocks.map((block,position)=>block.layout.group===group?position:-1).filter(position=>position>=0);return{start:Math.min(...indexes),end:Math.max(...indexes),items:blocks.slice(Math.min(...indexes),Math.max(...indexes)+1)}}
 function blockUnits(){const units=[];for(const block of blocks){const last=units[units.length-1];if(block.layout.group&&last?.[0]?.layout.group===block.layout.group)last.push(block);else units.push([block])}return units}
 function repairGroup(group){if(!group)return;const members=blocks.filter(block=>block.layout.group===group);if(members.length<2)members.forEach(block=>{block.layout.group=null;block.layout.mode='stack'})}
 function move(index,direction){const unit=unitAt(index);if(direction<0&&unit.start===0||direction>0&&unit.end===blocks.length-1)return;const neighbour=unitAt(direction<0?unit.start-1:unit.end+1),start=Math.min(unit.start,neighbour.start),count=unit.items.length+neighbour.items.length,replacement=direction<0?[...unit.items,...neighbour.items]:[...neighbour.items,...unit.items];blocks.splice(start,count,...replacement);setDirty();render()}
 function duplicateBlock(block,index){if(catalog[block.type].singleton){toast('warning',catalog[block.type].label+' can only be added once.');return}const copy=clone(block),unit=unitAt(index);copy.uid=uuid();copy.global_section_id=null;copy.global_name='';copy.layout=defaultLayout();blocks.splice(unit.end+1,0,copy);open.add(copy.uid);setDirty();render()}
 function freshBlock(type){const definition=catalog[type],localized={};languages.forEach(language=>localized[language.locale]=localeDefaults(definition,language.locale));return{uid:uuid(),type,source:definition.source,global_section_id:null,global_name:'',global_version:0,visible:true,visible_from:'',visible_until:'',layout:defaultLayout(),appearance:defaultAppearance(),shared:clone(definition.shared_defaults),localized}}
 function addBlock(type){const definition=catalog[type];if(definition.singleton&&blocks.some(block=>block.type===type)){toast('warning',definition.label+' is already on this page.');return}const block=freshBlock(type);blocks.push(block);open.clear();open.add(block.uid);setDirty();render();setTimeout(()=>list.querySelector('[data-uid="'+CSS.escape(block.uid)+'"]')?.scrollIntoView({behavior:'smooth',block:'center'}),30)}
 function addGlobal(global){if(!catalog[global.type]){toast('warning','This shared section type is unavailable.');return}if(blocks.some(block=>block.global_section_id===Number(global.id))){toast('warning','This shared section is already on the page.');return}if(catalog[global.type].singleton&&blocks.some(block=>block.type===global.type)){toast('warning',catalog[global.type].label+' can only be added once.');return}const block=normalize({uid:uuid(),type:global.type,source_theme:global.source_theme,global_section_id:global.id,global_name:global.name,visible:true,shared:global.shared,localized:global.localized});blocks.push(block);open.clear();open.add(block.uid);setDirty();render()}
 function renderLibrary(){const target=root.querySelector('[data-builder-library]'),query=root.querySelector('[data-builder-search]').value.toLocaleLowerCase(),groups={};Object.entries(catalog).forEach(([type,item])=>{if(query&&!`${item.label} ${item.description} ${item.group}`.toLocaleLowerCase().includes(query))return;(groups[item.group]??=[]).push([type,item])});target.replaceChildren(...Object.entries(groups).map(([group,items])=>{const section=document.createElement('section');section.className='sensecms-builder-library-group';const title=document.createElement('strong');title.textContent=group;section.append(title);items.forEach(([type,item])=>{const add=document.createElement('button');add.type='button';add.className='sensecms-builder-library-item';add.disabled=item.singleton&&blocks.some(block=>block.type===type);const tile=document.createElement('i');tile.append(icon(item.icon));const copy=document.createElement('span'),name=document.createElement('strong'),description=document.createElement('span'),source=document.createElement('small');name.textContent=item.label;description.textContent=add.disabled?'Already added':item.description;source.textContent=item.source_label||'Module';copy.append(name,description,source);add.append(tile,copy,icon(add.disabled?'check':'plus'));add.onclick=()=>addBlock(type);section.append(add)});return section}));window.lucide?.createIcons()}
 function compatiblePattern(pattern){const reserved=new Set(blocks.filter(block=>catalog[block.type]?.singleton).map(block=>block.type)),room=Math.max(0,60-blocks.length),types=[];let skipped=0;for(const type of pattern.sections||[]){const definition=catalog[type];if(!definition||types.length>=room||(definition.singleton&&reserved.has(type))){skipped++;continue}types.push(type);if(definition.singleton)reserved.add(type)}return{types,skipped}}
 function insertPattern(pattern){const plan=compatiblePattern(pattern);if(!plan.types.length){toast('warning',blocks.length>=60?'A page can contain up to 60 sections.':'Every compatible section from this pattern is already on the page.');return}const apply=()=>{const added=plan.types.map(freshBlock);blocks.push(...added);open=new Set(added.map(block=>block.uid));selected.clear();setDirty();render();setTimeout(()=>list.querySelector('[data-uid="'+CSS.escape(added[0].uid)+'"]')?.scrollIntoView({behavior:'smooth',block:'center'}),30);toast('success',pattern.label+' added · '+added.length+' '+(added.length===1?'section':'sections')+(plan.skipped?' · '+plan.skipped+' conflict'+(plan.skipped===1?'':'s')+' skipped':'')+'.')};const message='This will append '+plan.types.length+' independent, fully editable '+(plan.types.length===1?'section':'sections')+' to the page.'+(plan.skipped?' '+plan.skipped+' conflicting or unavailable '+(plan.skipped===1?'section will':'sections will')+' be skipped.':'');if(window.SenseCMSUI?.modal)window.SenseCMSUI.modal('Add '+pattern.label+' pattern',message,apply);else apply()}
 function renderPatterns(){const target=root.querySelector('[data-builder-patterns]'),input=root.querySelector('[data-builder-pattern-search]');if(!target||!input)return;const query=input.value.toLocaleLowerCase(),groups={};patterns.forEach(pattern=>{if(query&&!`${pattern.label} ${pattern.description} ${pattern.group} ${(pattern.sections||[]).map(type=>catalog[type]?.label||type).join(' ')}`.toLocaleLowerCase().includes(query))return;(groups[pattern.group||'Page layouts']??=[]).push(pattern)});if(!Object.keys(groups).length){target.innerHTML='<div class="sensecms-builder-global-empty"><i data-lucide="search-x"></i><strong>No matching patterns</strong><span>Try a page purpose, section name or category.</span></div>';window.lucide?.createIcons();return}target.replaceChildren(...Object.entries(groups).map(([group,items])=>{const section=document.createElement('section');section.className='sensecms-builder-library-group';const title=document.createElement('strong');title.textContent=group;section.append(title);items.forEach(pattern=>{const plan=compatiblePattern(pattern),card=document.createElement('article');card.className='sensecms-builder-pattern';const head=document.createElement('header'),tile=document.createElement('i'),copy=document.createElement('div'),name=document.createElement('strong'),description=document.createElement('span');tile.append(icon(pattern.icon||'layout-template'));name.textContent=pattern.label;description.textContent=pattern.description;copy.append(name,description);head.append(tile,copy);const sequence=document.createElement('div');sequence.className='sensecms-builder-pattern-sequence';(pattern.sections||[]).forEach(type=>{if(!catalog[type])return;const item=document.createElement('span');item.append(icon(catalog[type].icon),document.createTextNode(catalog[type].label));sequence.append(item)});const footer=document.createElement('footer'),meta=document.createElement('small'),add=document.createElement('button');meta.textContent=(pattern.sections||[]).length+' section'+((pattern.sections||[]).length===1?'':'s')+(plan.skipped?' · '+plan.skipped+' skipped':'');add.type='button';add.disabled=!plan.types.length;add.append(icon(plan.types.length?'plus':'check'),document.createTextNode(plan.types.length?'Insert pattern':'Already included'));add.onclick=()=>insertPattern(pattern);footer.append(meta,add);card.append(head,sequence,footer);section.append(card)});return section}));window.lucide?.createIcons()}
 function renderGlobals(){const target=root.querySelector('[data-builder-global-library]'),query=root.querySelector('[data-builder-global-search]').value.toLocaleLowerCase();root.querySelector('[data-builder-global-count]').textContent=String(globals.length);const items=globals.filter(item=>!query||`${item.name} ${catalog[item.type]?.label||item.type}`.toLocaleLowerCase().includes(query));if(!items.length){target.innerHTML='<div class="sensecms-builder-global-empty"><i data-lucide="combine"></i><strong>'+(globals.length?'No matching shared sections':'No shared sections yet')+'</strong><span>Save a configured page section to reuse it across the website.</span></div>';window.lucide?.createIcons();return}target.replaceChildren(...items.map(item=>{const row=document.createElement('div');row.className='sensecms-builder-library-item';const tile=document.createElement('i');tile.append(icon(catalog[item.type]?.icon||'panel-top'));const copy=document.createElement('span'),name=document.createElement('strong'),description=document.createElement('span'),source=document.createElement('small'),actions=document.createElement('div'),add=document.createElement('button'),rename=document.createElement('button'),archive=document.createElement('button');name.textContent=item.name;description.textContent=(catalog[item.type]?.label||item.type)+' · used on '+Number(item.usage_count||0)+' page section'+(Number(item.usage_count)===1?'':'s');source.textContent='Shared';copy.append(name,description,source);actions.className='sensecms-builder-global-actions';add.type=rename.type=archive.type='button';add.title='Add shared section';add.append(icon(blocks.some(block=>block.global_section_id===Number(item.id))?'check':'plus'));add.disabled=blocks.some(block=>block.global_section_id===Number(item.id));add.onclick=()=>addGlobal(item);rename.title='Rename shared section';rename.append(icon('pencil'));rename.onclick=()=>openRename(item);archive.title='Archive shared section';archive.className='is-danger';archive.append(icon('trash-2'));archive.onclick=()=>archiveGlobal(item);actions.append(add,rename,archive);row.append(tile,copy,actions);return row}));window.lucide?.createIcons()}
 function localeNavigation(){const nav=root.querySelector('[data-builder-locales]');nav.replaceChildren(...languages.map(language=>{const tab=document.createElement('button');tab.type='button';tab.className=language.locale===activeLocale?'is-active':'';tab.setAttribute('aria-pressed',String(language.locale===activeLocale));if(language.flag){const flag=document.createElement('img');flag.src=language.flag;flag.alt='';tab.append(flag)}const label=document.createElement('b');label.textContent=language.native_name||language.name||language.locale;const code=document.createElement('span');code.textContent=language.locale.toUpperCase();tab.append(label,code);tab.onclick=()=>{activeLocale=language.locale;localeNavigation();render();if(liveMode)refreshLivePreview()};return tab}));}
 function openDialog(dialog){if(!dialog)return;dialog.hidden=false;document.body.style.overflow='hidden';dialog.querySelector('input,button')?.focus();window.lucide?.createIcons()}
 function closeDialog(dialog){if(!dialog)return;dialog.hidden=true;if(!root.querySelector('.sensecms-builder-modal:not([hidden])'))document.body.style.overflow=''}
 function offerRecovery(){const draft=payload.draft;if(!draft||!Array.isArray(draft.blocks))return;const recovered=draft.blocks.filter(block=>catalog[block.type]).map(normalize),recoveredFingerprint=JSON.stringify(recovered.map(({uid,type,source,global_section_id,global_version,visible,visible_from,visible_until,layout,appearance,shared,localized})=>({uid,type,source,global_section_id,global_version,visible,visible_from,visible_until,layout,appearance,shared,localized})));if(recoveredFingerprint===savedFingerprint){discardDraft(true);return}const dialog=root.querySelector('[data-builder-recovery-dialog]'),meta=dialog.querySelector('[data-builder-recovery-meta]'),warning=dialog.querySelector('[data-builder-recovery-warning]');meta.textContent='Saved '+new Date(String(draft.updated_at).replace(' ','T')).toLocaleString()+' · '+recovered.length+' section'+(recovered.length===1?'':'s');warning.hidden=Number(draft.base_version)===version;dialog.querySelector('[data-builder-recovery-restore]').onclick=()=>{blocks=recovered;draftFingerprint=recoveredFingerprint;draftSavedAt=draft.updated_at||'';history=history.slice(0,historyIndex+1);history.push(recoveredFingerprint);historyIndex=history.length-1;open=new Set(blocks[0]?[blocks[0].uid]:[]);closeDialog(dialog);render();setDirty(true,{remember:false,recover:false});updateHistoryControls();toast('success','Recovery draft restored. Save changes when you are ready.')};dialog.querySelector('[data-builder-recovery-discard]').onclick=async()=>{const control=dialog.querySelector('[data-builder-recovery-discard]');control.disabled=true;await discardDraft();control.disabled=false;closeDialog(dialog);toast('success','Recovery draft discarded.')};openDialog(dialog)}
 function layoutMembers(block){return block.layout.group?blocks.filter(item=>item.layout.group===block.layout.group):[block]}
 function openLayout(block){layoutTarget=block;const dialog=root.querySelector('[data-builder-layout-dialog]'),form=dialog.querySelector('[data-builder-layout-form]'),layout=block.layout,members=layoutMembers(block);dialog.querySelector('#builder-layout-title').textContent=catalog[block.type].label+' layout';form.elements.container.value=layout.container;form.elements.gap.value=layout.gap;form.elements.align.value=layout.align;form.elements.wrap.checked=layout.wrap;for(const point of ['desktop','tablet','mobile']){form.elements[point+'_span'].value=String(layout[point].span);form.elements[point+'_order'].value=String(layout[point].order);form.elements[point+'_hidden'].checked=layout[point].hidden}const ungroup=dialog.querySelector('[data-builder-layout-ungroup]');ungroup.hidden=!layout.group;ungroup.innerHTML='<i data-lucide="ungroup"></i>Remove '+members.length+'-section group';openDialog(dialog)}
 function openShare(block){shareTarget=block;renameTarget=null;const dialog=root.querySelector('[data-builder-share-dialog]'),input=dialog.querySelector('[name=name]');dialog.querySelector('#builder-share-title').textContent='Create shared section';dialog.querySelector('[type=submit]').lastChild.textContent=' Create shared section';input.value=blockTitle(block);openDialog(dialog);input.select()}
 function openRename(global){shareTarget=null;renameTarget=global;const dialog=root.querySelector('[data-builder-share-dialog]'),input=dialog.querySelector('[name=name]');dialog.querySelector('#builder-share-title').textContent='Rename shared section';dialog.querySelector('[type=submit]').lastChild.textContent=' Save name';input.value=global.name;openDialog(dialog);input.select()}
 async function archiveGlobal(global){window.SenseCMSUI?.modal('Archive shared section','The section can only be archived when it is not linked to a page.',async()=>{try{await request('/content/builder/globals/'+global.id+'/archive',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf})});globals=globals.filter(item=>Number(item.id)!==Number(global.id));renderGlobals();toast('success','Shared section archived.')}catch(error){toast('error',error.message)}})}
 function renderRevisions(){const target=root.querySelector('[data-builder-revisions]'),compare=root.querySelector('[data-builder-compare]'),hint=root.querySelector('[data-builder-compare-hint]');revisionSelection=new Set([...revisionSelection].filter(id=>revisions.some(item=>Number(item.id)===id)));if(!revisions.length){target.innerHTML='<div class="sensecms-builder-revisions-empty">The first revision will be created when you save this page.</div>';compare.disabled=true;return}target.replaceChildren(...revisions.map(item=>{const row=document.createElement('article');row.className='sensecms-builder-revision'+(revisionSelection.has(Number(item.id))?' is-selected':'');const choice=document.createElement('input');choice.type='checkbox';choice.checked=revisionSelection.has(Number(item.id));choice.setAttribute('aria-label','Select version '+item.version+' for comparison');choice.onchange=()=>{const id=Number(item.id);if(choice.checked){if(revisionSelection.size>=2){choice.checked=false;toast('warning','Choose exactly two revisions to compare.');return}revisionSelection.add(id)}else revisionSelection.delete(id);renderRevisions()};const tile=document.createElement('i');tile.append(icon('history'));const copy=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('span'),restore=document.createElement('button');name.textContent='Version '+item.version+(Number(item.version)===version?' · current':'');meta.textContent=(item.author||'System')+' · '+new Date(String(item.created_at).replace(' ','T')).toLocaleString();copy.append(name,meta);restore.type='button';restore.textContent=Number(item.version)===version?'Current':'Restore';restore.disabled=!canEdit||Number(item.version)===version;restore.onclick=()=>restoreRevision(item);row.append(choice,tile,copy,restore);return row}));compare.disabled=revisionSelection.size!==2;hint.textContent=revisionSelection.size===2?'Ready to compare selected versions':revisionSelection.size===1?'Select one more revision':'Select two revisions to compare';window.lucide?.createIcons()}
 function renderRevisionComparison(comparison){const target=root.querySelector('[data-builder-revision-comparison]'),diff=comparison.diff||{},summary=diff.summary||{},head=document.createElement('header');target.hidden=false;head.innerHTML='<div><small>VERSION COMPARISON</small><strong>Version '+comparison.from.version+' → '+comparison.to.version+'</strong><span>'+comparison.from.author+' to '+comparison.to.author+'</span></div><button type="button" aria-label="Close comparison"><i data-lucide="x"></i></button>';head.querySelector('button').onclick=()=>{target.hidden=true};const stats=document.createElement('div');stats.className='sensecms-builder-diff-stats';[['added','Added'],['removed','Removed'],['changed','Changed'],['moved','Moved']].forEach(([key,label])=>{const item=document.createElement('span');item.innerHTML='<b>'+Number(summary[key]||0)+'</b><small>'+label+'</small>';stats.append(item)});const changes=document.createElement('div');changes.className='sensecms-builder-diff-list';if(diff.empty)changes.innerHTML='<div class="sensecms-builder-revisions-empty">These revisions contain the same section document.</div>';else(diff.changes||[]).forEach(change=>{const row=document.createElement('article');row.className='is-'+change.kind;const tile=document.createElement('i');tile.append(icon(change.kind==='added'?'plus':change.kind==='removed'?'minus':change.kind==='moved'?'move-vertical':'pencil-line'));const copy=document.createElement('div'),name=document.createElement('strong'),meta=document.createElement('span');name.textContent=change.label;meta.textContent=(change.details||[]).join(' · ');copy.append(name,meta);row.append(tile,copy);changes.append(row)});target.replaceChildren(head,stats,changes);window.lucide?.createIcons()}
 async function compareRevisions(){if(revisionSelection.size!==2)return;const ids=[...revisionSelection];try{const result=await request('/content/builder/'+payload.page.id+'/revisions/compare',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,from:ids[0],to:ids[1]})});renderRevisionComparison(result.data)}catch(error){toast('error',error.message||'The revisions could not be compared.')}}
 function restoreRevision(item){if(!canEdit)return;if(dirty){toast('warning','Save or discard current changes before restoring a revision.');return}window.SenseCMSUI?.modal('Restore version '+item.version,'The selected version will be restored as a new editable revision. Shared sections in the snapshot become independent copies.',async()=>{try{const result=await request('/content/builder/'+payload.page.id+'/revisions/'+item.id+'/restore',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,version})});version=Number(result.data.version);revisions=result.data.revisions||[];blocks=(result.data.document?.blocks||[]).filter(block=>catalog[block.type]).map(normalize);savedFingerprint=fingerprint();draftFingerprint='';dirty=false;history=[savedFingerprint];historyIndex=0;open=new Set(blocks[0]?[blocks[0].uid]:[]);render();renderRevisions();updateHistoryControls();updateState();closeDialog(root.querySelector('[data-builder-history-dialog]'));toast('success',result.message)}catch(error){toast('error',error.message)}})}
 list.addEventListener('dragover',event=>{if(!dragUid)return;event.preventDefault();const target=event.target.closest('[data-uid]');list.querySelectorAll('.is-drop-target').forEach(item=>item.classList.remove('is-drop-target'));if(target&&target.dataset.uid!==dragUid)target.classList.add('is-drop-target')});list.addEventListener('drop',event=>{event.preventDefault();const target=event.target.closest('[data-uid]');if(!target||target.dataset.uid===dragUid)return;const units=blockUnits(),from=units.findIndex(unit=>unit.some(block=>block.uid===dragUid)),to=units.findIndex(unit=>unit.some(block=>block.uid===target.dataset.uid));if(from<0||to<0||from===to)return;const[moved]=units.splice(from,1);units.splice(to,0,moved);blocks=units.flat();setDirty();render()});
 root.querySelector('[data-builder-copy-selected]').onclick=()=>copyBlocks(blocks.filter(block=>selected.has(block.uid)));root.querySelector('[data-builder-paste]').onclick=pasteClipboard;root.querySelector('[data-builder-search]').addEventListener('input',renderLibrary);root.querySelector('[data-builder-pattern-search]').addEventListener('input',renderPatterns);root.querySelector('[data-builder-global-search]').addEventListener('input',renderGlobals);root.querySelectorAll('[data-library-tab]').forEach(tab=>tab.onclick=()=>switchLibraryTab(tab.dataset.libraryTab));const aiForm=root.querySelector('[data-builder-ai-form]');if(aiForm){aiForm.addEventListener('submit',event=>{event.preventDefault();runAiProposal(aiForm)});aiForm.elements.action.addEventListener('change',updateAiScopes);aiForm.elements.scope.addEventListener('change',updateAiTarget);aiForm.elements.locale.addEventListener('change',event=>{activeLocale=String(event.target.value);localeNavigation();render()})}root.querySelector('[data-builder-page]').addEventListener('change',event=>{const go=()=>location.assign('/content/builder?document='+encodeURIComponent(event.target.value));if(!dirty){go();return}const guard=window.SenseCMSUnsavedGuard;if(guard?.confirmLeave)guard.confirmLeave(go);else window.SenseCMSUI?.modal('Leave unsaved changes?','Your recent changes have not been saved.',go)});
 undoButton.onclick=()=>applyHistory(historyIndex-1);redoButton.onclick=()=>applyHistory(historyIndex+1);root.querySelector('[data-builder-preview]').onclick=()=>switchBuilderView('live');root.querySelector('[data-builder-quality-open]').onclick=()=>switchLibraryTab('quality');root.querySelectorAll('[data-builder-view]').forEach(control=>control.onclick=()=>switchBuilderView(control.dataset.builderView));root.querySelectorAll('[data-live-device]').forEach(control=>control.onclick=()=>{root.querySelectorAll('[data-live-device]').forEach(item=>item.classList.toggle('is-active',item===control));root.querySelector('[data-live-stage]').dataset.liveStage=control.dataset.liveDevice});root.querySelectorAll('[data-preview-device]').forEach(control=>control.onclick=()=>{root.querySelectorAll('[data-preview-device]').forEach(item=>item.classList.toggle('is-active',item===control));root.querySelector('[data-preview-stage]').dataset.previewStage=control.dataset.previewDevice});liveFrame.addEventListener('load',()=>{setLiveStatus('Live Canvas ready');highlightLive()});addEventListener('message',event=>{if(event.origin!==location.origin||event.source!==liveFrame.contentWindow)return;const data=event.data||{};if(data.type==='sensecms-builder-select')selectCanvasBlock(String(data.uid||''));else if(data.type==='sensecms-builder-action')runLiveAction(String(data.uid||''),String(data.action||''));else if(data.type==='sensecms-builder-ready'){setLiveStatus('Live Canvas ready');highlightLive()}},{signal:lifecycle.signal});root.querySelector('[data-builder-history]').onclick=()=>{renderRevisions();openDialog(root.querySelector('[data-builder-history-dialog]'))};root.querySelector('[data-builder-compare]').onclick=compareRevisions;root.querySelectorAll('[data-dialog-close]').forEach(control=>control.onclick=()=>closeDialog(control.closest('.sensecms-builder-modal')));root.querySelectorAll('.sensecms-builder-modal:not([data-builder-recovery-dialog])').forEach(dialog=>dialog.addEventListener('mousedown',event=>{if(event.target===dialog)closeDialog(dialog)}));
 const mediaSearch=root.querySelector('[data-media-search]');mediaSearch.addEventListener('input',()=>{clearTimeout(mediaTimer);mediaTimer=setTimeout(()=>{mediaPage=1;loadMedia()},250)});root.querySelector('[data-media-kind]').addEventListener('change',()=>{mediaPage=1;loadMedia()});
 root.querySelector('[data-builder-share-form]').addEventListener('submit',async event=>{event.preventDefault();if(!shareTarget&&!renameTarget)return;const form=event.currentTarget,submit=form.querySelector('[type=submit]'),name=new FormData(form).get('name');submit.disabled=true;try{if(renameTarget){const result=await request('/content/builder/globals/'+renameTarget.id+'/rename',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,name})});globals=result.data.globals||globals;blocks.filter(block=>block.global_section_id===Number(renameTarget.id)).forEach(block=>block.global_name=String(name));closeDialog(root.querySelector('[data-builder-share-dialog]'));render();toast('success',result.message)}else{const result=await request('/content/builder/globals',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,name,block:shareTarget})});globals.unshift(result.data);shareTarget.global_section_id=Number(result.data.id);shareTarget.global_name=result.data.name;shareTarget.global_version=Number(result.data.version||0);closeDialog(root.querySelector('[data-builder-share-dialog]'));setDirty();render();toast('success',result.message)}}catch(error){toast('error',error.message)}finally{submit.disabled=false}});
 const layoutForm=root.querySelector('[data-builder-layout-form]');layoutForm.addEventListener('submit',event=>{event.preventDefault();if(!layoutTarget)return;const data=new FormData(layoutForm),members=layoutMembers(layoutTarget);for(const block of members){block.layout.container=String(data.get('container'));block.layout.gap=String(data.get('gap'));block.layout.align=String(data.get('align'));block.layout.wrap=data.has('wrap')}for(const point of ['desktop','tablet','mobile'])layoutTarget.layout[point]={span:Math.max(1,Math.min(12,Number(data.get(point+'_span'))||12)),order:Math.max(-20,Math.min(20,Number(data.get(point+'_order'))||0)),hidden:data.has(point+'_hidden')};closeDialog(root.querySelector('[data-builder-layout-dialog]'));setDirty();render();toast('success','Responsive layout updated.')});root.querySelector('[data-builder-layout-ungroup]').onclick=()=>{if(!layoutTarget?.layout.group)return;const members=layoutMembers(layoutTarget);members.forEach(block=>block.layout=defaultLayout());closeDialog(root.querySelector('[data-builder-layout-dialog]'));setDirty();render();toast('success','Layout group removed. Sections are independent again.')};
 saveButton.addEventListener('click',async()=>{if(saving||!dirty)return;clearTimeout(draftTimer);clearTimeout(draftMaxTimer);draftMaxTimer=0;saving=true;saveError=false;updateState();try{const result=await request('/content/builder/'+payload.page.id,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:payload.csrf,version,blocks:blockPayload()})});version=Number(result.data.version);revisions=result.data.revisions||revisions;globals=result.data.globals||globals;globals.forEach(global=>blocks.filter(block=>block.global_section_id===Number(global.id)).forEach(block=>block.global_version=Number(global.version||0)));savedFingerprint=fingerprint();draftFingerprint='';draftSavedAt='';dirty=false;render();if(liveMode)refreshLivePreview();toast('success',result.message)}catch(error){saveError=true;toast('error',error.message||'The page could not be saved.')}finally{saving=false;updateState()}});
 document.addEventListener('keydown',event=>{if(!canEdit||!(event.ctrlKey||event.metaKey)||event.altKey)return;const target=event.target;if(target instanceof HTMLElement&&(target.matches('input,textarea,select')||target.isContentEditable))return;const key=event.key.toLowerCase();if(key==='z'){event.preventDefault();applyHistory(event.shiftKey?historyIndex+1:historyIndex-1)}else if(key==='y'){event.preventDefault();applyHistory(historyIndex+1)}},{signal:lifecycle.signal});addEventListener('pagehide',()=>{if(dirty&&fingerprint()!==draftFingerprint)flushDraft({keepalive:true,quiet:true});if(lockedUid)fetch('/content/builder/'+payload.page.id+'/sections/'+encodeURIComponent(lockedUid)+'/unlock',{method:'POST',keepalive:true,headers:{'Content-Type':'application/json','X-SenseCMS-Request':'1',Accept:'application/json'},body:JSON.stringify({csrf:payload.csrf})}).catch(()=>{})},{signal:lifecycle.signal});addEventListener('beforeunload',event=>{if(!dirty||window.SenseCMSUnsavedGuard?.isBypassing())return;event.preventDefault();event.returnValue=''},{signal:lifecycle.signal});savedFingerprint=fingerprint();qualityFingerprint=savedFingerprint;history=[savedFingerprint];historyIndex=0;localeNavigation();setDirty(false,{remember:false,recover:false});updateHistoryControls();render();offerRecovery();syncCollaboration(true);
};
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();document.addEventListener('sensecms:navigated',boot);
})();
